I will save all my practical work in this repository 




useful: 

""" 
ssh-keygen -t rsa -b 4096 -C "your_email@example.com" -f $HOME/.ssh/id_rsa 

eval "$(ssh-agent -s)" 

ssh-add ~/.ssh/id_ed25519

"""